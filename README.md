# 🛡️ Simple PHP Login & Signup System

A clean and professional User Authentication System built with PHP and MySQL, featuring a modern sliding UI.

## 🚀 Key Features
* **Form Switching:** Smooth CSS animations to switch between Login and Signup.
* **Secure Auth:** PHP backend to handle user registration and login securely.
* **Session Management:** Users are redirected to a protected dashboard upon successful login.
* **Database Driven:** Full integration with MySQL database for user data storage.

## 🛠️ Tech Stack
* **Frontend:** HTML5, CSS3 (Custom styling), JavaScript
* **Backend:** PHP (Server-side logic)
* **Database:** MySQL

## 📖 Setup Instructions
1. Download the code or clone the repository.
2. Import `conn.sql` to your local MySQL server (XAMPP).
3. Update `db.php` with your database details (leave password blank if local).
4. Launch `index.php` in your browser.
